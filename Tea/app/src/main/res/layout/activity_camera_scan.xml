<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- Camera Preview -->
    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Dark Overlay for contrast -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#2B000000" />

    <!-- Top Control Bar with Mode Indicators -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:background="#1AFFFFFF"
        android:elevation="10dp">

        <!-- Scan Mode Indicator -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/scanModeIndicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/scan_smart"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/white"
                android:layout_marginEnd="8dp" />

            <com.google.android.material.chip.Chip
                android:id="@+id/modeChip"
                style="@style/Widget.MaterialComponents.Chip.Action"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📷 Image"
                android:textSize="11sp"
                android:textColor="@color/white"
                app:chipBackgroundColor="#66FFFFFF"
                app:chipCornerRadius="8dp"
                app:chipMinHeight="28dp" />

        </LinearLayout>

        <!-- Action Buttons -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/flashButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_marginEnd="10dp"
                android:contentDescription="@string/camera_flash"
                app:backgroundTint="#33FFFFFF"
                app:cornerRadius="12dp"
                app:icon="@android:drawable/ic_menu_camera"
                app:iconTint="@color/white" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/closeButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:contentDescription="@string/camera_close"
                app:backgroundTint="#33FFFFFF"
                app:cornerRadius="12dp"
                app:icon="@android:drawable/ic_menu_close_clear_cancel"
                app:iconTint="@color/white" />

        </LinearLayout>

    </LinearLayout>

    <!-- Center Scan Frame -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center">

        <FrameLayout
            android:layout_width="280dp"
            android:layout_height="200dp"
            android:layout_gravity="center"
            android:background="@drawable/scan_frame_background"
            android:elevation="4dp" />

    </FrameLayout>

    <!-- Bottom Info Panel -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="#33000000">

        <!-- Quality Status -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="12dp"
            android:visibility="gone"
            android:id="@+id/qualityStatusLayout">

            <View
                android:id="@+id/qualityIndicator"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/quality_indicator_bg" />

            <TextView
                android:id="@+id/qualityStatusText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/quality_checking"
                android:textSize="12sp"
                android:textColor="#F5ECE1"
                android:alpha="0.9" />

        </LinearLayout>

        <!-- Main Scan Info -->
        <TextView
            android:id="@+id/scanInfoText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/camera_scan_info_default"
            android:textSize="15sp"
            android:textColor="#F5ECE1"
            android:textAlignment="center"
            android:layout_marginBottom="10dp"
            android:alpha="0.95" />

        <!-- Scanning Status -->
        <TextView
            android:id="@+id/scanResultText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/camera_scanning"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/primary_light_teal"
            android:textAlignment="center"
            android:alpha="0.98" />

        <!-- Progress Indicator -->
        <ProgressBar
            android:id="@+id/scanProgressBar"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:layout_marginTop="16dp"
            style="?android:attr/progressBarStyleHorizontal"
            android:indeterminate="true"
            android:indeterminateTint="@color/primary_light_teal" />

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
